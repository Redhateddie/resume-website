version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build required for plain HTML"
    build:
      commands:
        - echo "Copying files to artifact directory"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
